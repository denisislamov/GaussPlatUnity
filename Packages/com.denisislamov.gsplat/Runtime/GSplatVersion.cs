namespace GSplat
{
    /// <summary>
    /// The package version, for the debug overlay and bug reports. Must match "version" in package.json; an EditMode
    /// test checks that so the two cannot drift apart.
    /// </summary>
    public static class GSplatVersion
    {
        public const string Current = "0.2.1";
    }
}
